import React from 'react';
import Input from '../atoms/Input/Input';
import Label from '../atoms/labels/Label';

function InputsDesign({textLabel, typeInput, placeholder, name = ''}) {
  return (
   <div>
        {/* <div>
            <Label text={textLabel} />
        </div>
        <div>
            <Input type={typeInput} placeholder={placeholder} name =''/>
        </div> */}
    </div>
  )
}

export default InputsDesign;